import 'package:flutter/material.dart';

class DayPickWidget extends StatelessWidget {
  const DayPickWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
