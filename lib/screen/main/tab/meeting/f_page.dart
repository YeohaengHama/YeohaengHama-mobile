import 'package:flutter/material.dart';

class PageFragment extends StatelessWidget {
  const PageFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
